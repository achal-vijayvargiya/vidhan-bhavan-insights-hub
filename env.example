# Backend API Configuration
# Update with your actual FastAPI backend server URL
VITE_API_URL=http://103.112.121.174:8000/api

# Frontend Port Configuration
# Default port is 3030 to avoid conflicts with common services
FRONTEND_PORT=3030

# Optional HTTPS port (uncomment if you enable HTTPS)
# FRONTEND_HTTPS_PORT=8443

# Production Environment Variables
# Copy this file to .env and update the values according to your deployment

# Common Port Options:
# FRONTEND_PORT=3030  # Default - safe choice
# FRONTEND_PORT=3000  # Common React dev port
# FRONTEND_PORT=9000  # Alternative high port
# FRONTEND_PORT=8080  # Web proxy port
# FRONTEND_PORT=80    # Standard HTTP (may require sudo/root)

# Backend API URL Examples:
# For local development with backend running on same machine:
# VITE_API_URL=http://localhost:8000/api

# For production with backend on same server (recommended):
# VITE_API_URL=http://103.112.121.174:8000/api
# For HTTPS (if you have SSL configured):
# VITE_API_URL=https://103.112.121.174:8000/api

# If backend is running in Docker on same host:
# VITE_API_URL=http://host.docker.internal:8000/api